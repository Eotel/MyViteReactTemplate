import { useState } from 'react';
import './App.css';

const App = () => {
  return (
    <div className="App">
      <button type="button" className="btn btn-primary">
        Button
      </button>
    </div>
  );
};

export default App;
